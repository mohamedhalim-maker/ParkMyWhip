/// App configuration constants
class AppConfig {
  AppConfig._();

  /// App identifier in the `apps` table
  static const String appId = 'park_my_whip_tow_driver';

  /// Default role for users in this app
  static const String defaultRole = 'user';
}
